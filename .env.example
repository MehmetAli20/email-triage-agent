# Anthropic API key - first needed when the analyzer lands.
# The contract, the corpus and the baselines all run offline.
ANTHROPIC_API_KEY=

# Default model. Overridden during the cost ablation.
TRIAGE_MODEL=claude-haiku-4-5
